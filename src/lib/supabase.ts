/**
 * Single browser Supabase client — env only, no hardcoded project URLs.
 * Prefer importing from here or `@/integrations/supabase/client` (same instance).
 */
export { supabase } from "@/integrations/supabase/client";
