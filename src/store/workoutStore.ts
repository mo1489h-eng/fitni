/**
 * @file Re-export for consumers expecting `workoutStore` naming.
 * Implementation: `./workout-store`
 */
export {
  useWorkoutStore,
  type MuscleGroupId,
  type SyncJob,
  type SyncJobKind,
} from "./workout-store";
